<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/controlListIcon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginStart="16dp"
        android:layout_marginLeft="16dp"
        android:layout_marginTop="16dp"
        android:contentDescription="TODO"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_settings_remote_w" />

    <androidx.appcompat.widget.AppCompatSpinner
        android:id="@+id/channelMapSelectControlSpinner2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginLeft="24dp"
        android:paddingLeft="0dp"
        android:paddingRight="0dp"
        android:popupBackground="@color/grey_700"
        app:layout_constraintBottom_toBottomOf="@+id/controlListIcon"
        app:layout_constraintStart_toEndOf="@+id/controlListIcon"
        app:layout_constraintTop_toTopOf="@+id/controlListIcon" />
</androidx.constraintlayout.widget.ConstraintLayout>